package Builder;

import Core.NetworkTopology;

public class TopologyBuilder {
    public NetworkTopology buildTopology(String spec) {
        NetworkTopology topology = new NetworkTopology();
        
        return topology;
    }
}